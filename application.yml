---
# Items that apply to ALL profiles:
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka/
 
---
spring:
  profiles: rjpard-discovery-config-client-noun
words: boat,book,vote,seat,backpack,partition,groundhog
